import 'package:flutter/material.dart';

abstract class ResImage {
  static const IconData done = Icons.check_circle;
  static const IconData retry = Icons.refresh;
  static const IconData map = Icons.map;
  static const IconData chart = Icons.stacked_line_chart;
}